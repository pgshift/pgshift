{
  "name": "@pgshift/__tests__",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run unit",
    "test:integration": "vitest run integration",
    "test:watch": "vitest",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@pgshift/cache": "*",
    "@pgshift/core": "*",
    "@pgshift/search": "*"
  },
  "devDependencies": {
    "@pgshift/lint": "*",
    "@pgshift/ts": "*",
    "@types/dockerode": "4.0.1",
    "dockerode": "5.0.0",
    "vitest": "4.1.6"
  }
}
